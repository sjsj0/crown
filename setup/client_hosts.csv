# VMs that should run ONLY the client binary (benchmark / workload drivers).
# One host per line; a trailing comma is allowed. Lines starting with '#' and
# blank lines are ignored. The deployment automation builds the repo on the
# UNION of prod_hosts.csv (server nodes) and this file.
sp26-cs525-1219.cs.illinois.edu,
